OPENROUTER_API_KEY=your_openrouter_api_key_here

# Audiocraft inference device.
# "cpu"  → local/web instance (no GPU required, slower but fine for audiogen-small)
# "cuda" → Colab T4 GPU instance (set this in Colab for faster inference)
AUDIOCRAFT_DEVICE=cpu
