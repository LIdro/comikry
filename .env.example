OPENROUTER_API_KEY=your_openrouter_api_key_here

# Audiocraft inference device.
# "cpu"  → local/web instance (no GPU required, slower but fine for audiogen-small)
# "cuda" → Colab T4 GPU instance (set this in Colab for faster inference)
AUDIOCRAFT_DEVICE=cpu

# Optional: URL of a remote SFX API server (e.g. the Colab SFX server cell).
# When set, SFX generation will POST to this endpoint instead of running
# AudioGen locally. Leave empty to use local AudioGen inference.
# Example: SFX_API_URL=https://xxxx-colab-tunnel.ngrok.io
SFX_API_URL=

# Gemini Files API (optional, opt-in).
# When USE_GEMINI_FILES_API=true, page images are uploaded once to the Google
# Gemini Files API and referenced by URI for all vision calls. This avoids
# base64-encoding large images in every request (faster, lower token usage).
# Requires a Google AI API key. Leave false to use the default base64 path.
GOOGLE_AI_API_KEY=your_google_ai_api_key_here
USE_GEMINI_FILES_API=false
